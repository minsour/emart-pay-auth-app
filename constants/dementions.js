import { Dimensions } from 'react-native';

const DIMENSIONS_WIDTH = Dimensions.get("window").width;
const DIMENSIONS_HEIGHT = Dimensions.get("screen").height;

export {
    DIMENSIONS_WIDTH,
    DIMENSIONS_HEIGHT,
}